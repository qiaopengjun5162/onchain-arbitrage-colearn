# 2026-08-29（周六）— 清算事件线第 3 步落地

## 上午：状态盘点 + 雷达健康核验（✅）

- 结论反转：主雷达 v2 健康（--once 实测 12/12 全 ok，cbBTC-EURC EUR 交叉修复已生效，mGLO -638bps 偏安全）
- 之前看到的坏市场（HERMES BROKEN/USR scale?/?-WETH）全是 v1 旧脚本写的 prey_radar.jsonl——v1 无 EUR 修复 + 符号 bug
- 基础设施告警：微信 iLink DNS 全天解析失败（提醒全挂），部分 TG watchdog delivery error——脚本本身正常

## 主线：清算事件线第 3 步 = prey radar × HF 联动（完成 ✅）

- 新增 `scripts/prey_hf_trigger.py`：读 prey_radar_v2.jsonl 尾部信号（JUMP/SIGNAL/ORACLE_UPDATE/BROKEN_ORACLE）→ 立即触发持仓级 HF 扫描（import morpho_liquidation_hf.scan 复用）→ 输出「信号 + 谁 HF 最低 + 触发跌幅」
- 独立进程（不塞 v2 主循环）：事件驱动零开销；跨进程去重 10min/信号；watchdog 契约（无信号静默）
- cron `44acb25fa67a` 每 2 分钟（比 30min HF cron 快 15 倍）
- 实测：watchdog 静默 ✅ / 全量 HF 快照发现 USDe→USDC 贴线大仓（HF 1.006 起、触发跌幅 0.57%-1.93%，含 $49.32M）

## 顺手：v1 旧雷达退役（✅）

- v1 `morpho_prey_radar.py` 符号 bug：偏离用绝对值 → mGLO 型「oracle 低估偏安全」误报 SIGNAL；无 EUR 交叉
- 停 cron `6c9d49f28ba7`；v2 `--include-broken` 补位：加「有资金」过滤 + marketId 去重，watch 脚本更新
- **发现**：24 个 HERMES 市场 listed=False 但 supply≈$55M×19+$11M×5+$98M 全 util=100%、oracle 冻结（BROKEN_ORACLE）≈$1.2B+ 借款困在 frozen 市场 = 清算连环弹药库（24h 去重报一次）

## 下午：Flashblocks 决胜层——预言机更新监听器（完成 ✅）

- `scripts/chainlink_feed_watch.py`：核心方法论打通——MorphoChainlinkOracleV2 的 feeds 是 immutable（无 getter、不在 storage）→ **bytecode PUSH32 扫描提取 feed proxy**（USDe 型 46B 最小代理先穿透）→ `proxy.aggregator()` 拿 aggregator → pending 区块扫更新交易
- 12 市场 → 11 个 aggregator 全部验证（cbBTC-USDC/cbBTC-EURC 共享 BTC feed，cbETH 系共享 ETH feed，EURC/USD=0xc91d87e8，latestAnswer 全部对上）
- 命中=预言机刚更新=确定性清算窗口信号 → 立即 HF 联动（复用 prey_hf_trigger.hf_scan）
- cron `3791583377a0` 每 2 分钟 100s 窗口；真 <200ms 需 Chainstack 付费 WS（端点可换）
- 踩坑：公共 RPC 网络抖动 → 已加 3 次重试

## 下午：基础设施收尾（✅）

- **HERMES 日志污染修复**：--include-broken 后 24 个 frozen 市场每 60s 心跳写日志 → v2 日志从 7505 行暴涨 57932 行（21MB）→ 修：恒定 BROKEN_ORACLE 不写心跳（首次才写）
- **data/ 整目录停止跟踪**：1573 个运行时文件（含 21MB jsonl）误入库 → .gitignore 补 `data/*.jsonl` + `data/*.db`，git rm -r --cached
- 雷达健康复测：12/12 ok（含一次瞬时网络抖动自愈验证）

## 晚间：pre-IPO 观察清单核验 + v2 降噪修复（✅）

- **pre-IPO 清单核验**（事件日历 7 项）：Anthropic S-1 已交 6/1（$965B，Q4 2026 目标，设 expected_ipo 12/31 触发提醒）；OpenAI S-1 已交 6/8（$852B，Late 2026/2027）；Databricks IPO-ready 无 S-1（预期 Q3，$134B）；Stripe/Figure 无 S-1 2027-2028 低优先；**新增 Kraken（Q3 filed，加密交易所）+ Discord（H2 filed）**
- **v2 降噪二修**：第一版只挡「恒定 BROKEN 心跳」，漏了「BROKEN↔no_price 网络抖动跳变」——修复后 2h 数据 HERMES 27960 行（60% 噪声）→ 改：非正常态之间跳变不写日志（只有进出 ok 才写）；正常市场 ~1166 行/2h 符合预期
- 4206bps 实弹核实：非 radar 偏离，是 08-28 firsto 元件雷达跨市场价差（官方 0.021571→Firsto 0.030968，≈2 BNB 窗口）——D24 文章内容真实已发布
